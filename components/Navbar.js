import styles from '../styles/Home.module.css';

function Navbar() {
    return (
        <nav className={styles.nav}>
            <h3 className={styles.navHead}>CryptSeed</h3>
        </nav>
    )
}

export default Navbar
